# FullStackOpen


# 1. React components : Resuable UI elements for your app. First letter of React component names must be capitalized.

# 2. React Props : React components use props to communicate with each other. Every parent component can pass some information to its child components by giving them props. 

# 3. Fragements : <Fragment>, often used via <>...</> syntax, lets you group elements without a wrapper node.

